{
  "name": "chromakey-protocol",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "bootstrap": "npm install --prefix frontend",
    "start": "npm --prefix frontend start",
    "build": "npm --prefix frontend build",
    "test": "npm --prefix frontend test"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.2",
    "@supabase/supabase-js": "^2.104.1"
  }
}
